import { ThemeContext as SharedThemeContext } from './ThemeContext';

// Backward-compatible re-export for older imports.
// Kept as a constant-only file for react-refresh stability.
export const ThemeContext = SharedThemeContext;


